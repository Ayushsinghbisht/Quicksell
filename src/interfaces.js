// Example data structure for a Ticket
const Ticket = {
    id: '',
    title: '',
    tag: [],
    userId: '',
    status: '',
    priority: 0,
};

// Example data structure for a User
const User = {
    id: '',
    name: '',
    available: false,
};

// Example data structure for a Column (list of tickets)
const Col = {
    col: [],
};

// Example data structure for mapping UserId to User data
const UserIdToData = {
    userData: {},
};
